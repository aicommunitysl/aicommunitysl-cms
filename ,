                            )}}
                          >
                            <Pencil className=\"h-3.5 w-3.5\" />
                            Edit
                          </Link>
                        </Button>
                      ) : null}}
                      {{canDelete ? (
                        <Button
                          size=\"sm\"
                          variant=\"ghost\"
                          className=\"text-destructive hover:bg-[rgba(220,38,38,0.08)] hover:text-destructive\"
                          onClick={{() => void handleDelete(item)}}
                        >
                          <Trash2 className=\"h-3.5 w-3.5\" />
                          Delete
                        </Button>
                      ) : null}}
                    </div>
                  </div>
                ))
              )}}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}}
'''
with open('/home/dileepadev/Dev/aicommunitysl/aicommunitysl-cms/components/dashboard/resource-manager.tsx', 'w') as f:
    f.write(content)
print('written', len(content), 'chars')
"
